services:
  db:
    image: pgvector/pgvector:pg18
    environment:
      POSTGRES_DB: kbqa
      POSTGRES_USER: kbqa
      POSTGRES_PASSWORD: kbqa
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U kbqa"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql://kbqa:kbqa@db:5432/kbqa
      JWT_SECRET: dev-secret-change-in-production
      AWS_REGION: us-east-1
      S3_BUCKET_NAME: kbqa-documents
      ADMIN_USERNAME: admin
      ADMIN_EMAIL: admin@example.com
      ADMIN_PASSWORD: admin
      CORS_ORIGINS: "http://localhost:5173,http://localhost:3000"
    depends_on:
      db:
        condition: service_healthy

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend

volumes:
  pgdata:
